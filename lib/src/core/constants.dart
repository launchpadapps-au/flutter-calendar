import 'package:flutter/material.dart';

///animation duratipon for the scroll
const Duration animationDuration = Duration(milliseconds: 500);

///cureve for the scroll
const Curve animationCurve = Curves.easeIn;
